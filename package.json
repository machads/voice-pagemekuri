{
  "name": "voice-page-navigator",
  "version": "1.0.0",
  "description": "Voice-controlled page navigator",
  "scripts": {
    "dev": "npx http-server -p 8080 -S -C cert.pem -K key.pem",
    "start": "npx http-server -p 8080"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}